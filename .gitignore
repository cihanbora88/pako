# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Figma Make specific
src/imports/
# ^ Since we rebuilt all pages, we might want to ignore the original heavy Figma imports
# but for now let's keep it commented if the user wants to keep them in the repo.
# src/imports/
